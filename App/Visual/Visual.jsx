const Visual = (nome, icon, color, score) => {
return(<>
    <a href $ {...icon}></a>
     <h3>${nome}</h3>
    <style>${color}</style>
    <Number>${score}</Number>
   
   
    </>)
}
export default Visual